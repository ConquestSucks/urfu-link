apiVersion: kafka.strimzi.io/v1
kind: KafkaNodePool
metadata:
  name: kafka-broker
  namespace: urfu-platform
spec:
  storage:
    volumes:
      - id: 0
        type: persistent-claim
        size: 4Gi
        class: fast-ssd
        deleteClaim: false
